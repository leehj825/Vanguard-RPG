name: vanguard_path
description: A new Flame game project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flame: ^1.16.0
  flame_svg: ^1.11.18
  vector_math: ^2.1.4

  # CHANGED: Dependency name must match the package's internal name "stickman_3d"
  stickman_3d:
    git:
      url: https://github.com/leehj825/stickman-animator.git
      ref: main # CHANGED: Branch set to main

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/
